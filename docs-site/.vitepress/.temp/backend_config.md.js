import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Backend config","description":"","frontmatter":{},"headers":[],"relativePath":"backend/config.md","filePath":"backend/config.md","lastUpdated":1781673040000}');
const _sfc_main = { name: "backend/config.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="backend-config" tabindex="-1">Backend config <a class="header-anchor" href="#backend-config" aria-label="Permalink to &quot;Backend config&quot;">​</a></h1><p>This project uses a local YAML config file which must not be committed with real secrets.</p><ul><li>Main template: <code>backend/config/config.example.yaml</code></li><li>Public deployment template: <code>deploy/backend/config.public.example.yaml</code></li><li>Local runtime file: copy <code>backend/config/config.example.yaml</code> to <code>backend/config/config.yaml</code> and edit values</li><li>Backend config search paths: <code>.</code> / <code>./config</code> / <code>/etc/baobaobaivault</code></li><li>Environment variables are supported via Viper with prefix <code>BVAULT_</code></li></ul><h2 id="config-topics" tabindex="-1">Config topics <a class="header-anchor" href="#config-topics" aria-label="Permalink to &quot;Config topics&quot;">​</a></h2><h3 id="recommended-entry" tabindex="-1">Recommended entry <a class="header-anchor" href="#recommended-entry" aria-label="Permalink to &quot;Recommended entry&quot;">​</a></h3><ul><li>Primary frontend: <code>sharefrontend</code></li><li>Primary API path: <code>/api/share/*</code></li><li>New deployment and daily operations should follow the <code>sharefrontend + /api/share</code> architecture</li><li><code>server.admin_email</code> is the configured super admin identity used by the share system</li></ul><h3 id="web-push" tabindex="-1">Web Push <a class="header-anchor" href="#web-push" aria-label="Permalink to &quot;Web Push&quot;">​</a></h3><p>This repo can expose a standalone <code>mimiwebpushserver</code>-compatible Web Push API under <code>/api/*</code> when <code>webpush.enabled=true</code> and <code>webpush.public_api_enabled=true</code>.</p><ul><li>Generate VAPID keys: <code>go run ./cmd/webpushvapid</code></li><li>Main fields: <ul><li><code>webpush.enabled</code></li><li><code>webpush.public_api_enabled</code></li><li><code>webpush.vapid_public_key</code></li><li><code>webpush.vapid_private_key</code></li></ul></li></ul><h3 id="share-email-verification" tabindex="-1">Share email verification <a class="header-anchor" href="#share-email-verification" aria-label="Permalink to &quot;Share email verification&quot;">​</a></h3><ul><li>Main switch: <ul><li><code>share_auth.email_verification_enabled</code></li></ul></li><li>Main SMTP fields: <ul><li><code>email.enabled</code></li><li><code>email.from_name</code></li><li><code>email.from_address</code></li><li><code>email.smtp_host</code></li><li><code>email.smtp_port</code></li><li><code>email.smtp_username</code></li><li><code>email.smtp_password</code></li></ul></li><li>Detailed guides: <ul><li>English: <code>backend/config/SHARE_AUTH_EMAIL_DEPLOY.md</code></li><li>Chinese: <code>backend/config/SHARE_AUTH_EMAIL_DEPLOY_ZH.md</code></li></ul></li></ul><h3 id="share-media-storage-to-oss" tabindex="-1">Share media storage to OSS <a class="header-anchor" href="#share-media-storage-to-oss" aria-label="Permalink to &quot;Share media storage to OSS&quot;">​</a></h3><ul><li>The share card media storage switch is managed in the system UI, not in <code>config.yaml</code></li><li>System page: <ul><li><code>System Management -&gt; Media Storage</code></li><li>route: <code>/system/media-storage</code></li></ul></li><li>This switch only affects newly uploaded share card media</li><li>Historical local files are not migrated automatically in the first stage</li><li>Keep backend local storage mounted during rollout if you enable local fallback</li><li>In <code>object_storage</code> mode the browser uploads directly to OSS, so you must also configure: <ul><li>Bucket CORS (production domain and <code>http://localhost:3002</code> for local testing)</li><li>RAM permissions for the backend user: <code>oss:PutObject</code>, <code>oss:GetObject</code>, <code>oss:DeleteObject</code>, <code>oss:ListObjects</code></li><li>If using <code>acs:SourceIp</code> restrictions, whitelist both the backend server IP and your local development IP</li></ul></li><li>Detailed guides: <ul><li>English: <code>backend/config/SHARE_MEDIA_STORAGE_OSS_DEPLOY.md</code></li><li>Chinese: <code>backend/config/SHARE_MEDIA_STORAGE_OSS_DEPLOY_ZH.md</code></li></ul></li></ul><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><ul><li><code>share_auth.email_verification_enabled=false</code>: register creates and logs in directly</li><li><code>share_auth.email_verification_enabled=true</code>: register must verify email code first</li><li>For Aliyun Direct Mail, use the same main config templates above and fill <code>email.*</code>; no separate provider template is kept</li><li>The local development CORS defaults now target the Next.js share frontend on <code>http://localhost:3002</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("backend/config.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const config = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  config as default
};
