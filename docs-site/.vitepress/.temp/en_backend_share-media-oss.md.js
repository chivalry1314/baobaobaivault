import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Share Media Storage to OSS Guide","description":"","frontmatter":{},"headers":[],"relativePath":"en/backend/share-media-oss.md","filePath":"en/backend/share-media-oss.md","lastUpdated":1781673040000}');
const _sfc_main = { name: "en/backend/share-media-oss.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="share-media-storage-to-oss-guide" tabindex="-1">Share Media Storage to OSS Guide <a class="header-anchor" href="#share-media-storage-to-oss-guide" aria-label="Permalink to &quot;Share Media Storage to OSS Guide&quot;">​</a></h1><p>This guide explains how to move <code>sharefrontend</code> card covers and card attachments from local disk storage to object storage in a controlled way.</p><h2 id="_1-what-this-switch-does" tabindex="-1">1. What This Switch Does <a class="header-anchor" href="#_1-what-this-switch-does" aria-label="Permalink to &quot;1. What This Switch Does&quot;">​</a></h2><p>The media storage switch only affects newly uploaded share card media:</p><ul><li>card cover images</li><li>card attachment files in fixed slots</li></ul><p>It does not automatically migrate historical local files.</p><p>Existing files continue to be read according to the storage location recorded on each card or asset row:</p><ul><li>old local files still read from local disk</li><li>new OSS files read from object storage</li></ul><p>This design allows gradual rollout and rollback.</p><h2 id="_2-what-you-need-first" tabindex="-1">2. What You Need First <a class="header-anchor" href="#_2-what-you-need-first" aria-label="Permalink to &quot;2. What You Need First&quot;">​</a></h2><p>Before switching media writes to OSS, prepare these pieces:</p><ol><li>A working object storage provider configuration in system management</li><li>At least one namespace for covers</li><li>At least one namespace for attachments</li><li>Backend file read/write still available for old local files during transition</li><li>The object storage bucket has CORS configured (required for browser direct upload)</li><li>The RAM sub-account used by the backend has sufficient permissions</li></ol><p>In the current implementation, media storage mode is configured in the system management UI, not in <code>config.yaml</code>.</p><h3 id="_2-1-oss-cors-configuration" tabindex="-1">2.1 OSS CORS Configuration <a class="header-anchor" href="#_2-1-oss-cors-configuration" aria-label="Permalink to &quot;2.1 OSS CORS Configuration&quot;">​</a></h3><p>Because <code>object_storage</code> mode lets the browser PUT files directly to OSS, the bucket must have CORS configured.</p><p>Aliyun console URL template:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://oss.console.aliyun.com/bucket/oss-&lt;region&gt;/&lt;bucket-name&gt;/data-security/cors</span></span></code></pre></div><p>Recommended rule:</p><ul><li>Origin: <ul><li>Production: <code>https://your-domain</code></li><li>Local testing: <code>http://localhost:3002</code></li></ul></li><li>Allowed Methods: <code>GET</code>, <code>PUT</code>, <code>POST</code>, <code>HEAD</code></li><li>Allowed Headers: <code>Content-Type</code>, <code>*</code></li><li>Exposed Headers: <code>ETag</code></li><li>Cache Time: <code>300</code></li></ul><p>Note: For local testing the origin must include the protocol and port, e.g. <code>http://localhost:3002</code>.</p><h3 id="_2-2-ram-permissions" tabindex="-1">2.2 RAM Permissions <a class="header-anchor" href="#_2-2-ram-permissions" aria-label="Permalink to &quot;2.2 RAM Permissions&quot;">​</a></h3><p>The RAM sub-account used by the backend needs at least these actions:</p><ul><li><code>oss:PutObject</code>: generate presigned upload URLs</li><li><code>oss:GetObject</code>: read / stat objects</li><li><code>oss:DeleteObject</code>: delete objects (used during rollback, replacement, and card deletion)</li><li><code>oss:ListObjects</code>: some management operations</li></ul><p>Minimal policy example:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;Version&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;1&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;Statement&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;Effect&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Allow&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;Action&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        &quot;oss:PutObject&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        &quot;oss:GetObject&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        &quot;oss:DeleteObject&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        &quot;oss:ListObjects&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      ],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;Resource&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        &quot;acs:oss:*:*:baobaobaibucket&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        &quot;acs:oss:*:*:baobaobaibucket/*&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      ]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>If you restrict access with an <code>acs:SourceIp</code> condition, note that:</p><ul><li>Backend requests to OSS come from the backend server egress IP</li><li>Browser direct uploads during local development come from your local machine IP</li><li>During local testing either remove the <code>acs:SourceIp</code> condition or whitelist both the backend server IP and your local development IP</li><li>Otherwise you will see <code>403 AccessDenied: You have no right to access this object because of bucket acl</code></li></ul><h2 id="_3-what-still-uses-local-config" tabindex="-1">3. What Still Uses Local Config <a class="header-anchor" href="#_3-what-still-uses-local-config" aria-label="Permalink to &quot;3. What Still Uses Local Config&quot;">​</a></h2><p>Even though media mode is not stored in YAML, these backend config fields still matter:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">storage</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  default_provider</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;local&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  temp_dir</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;/tmp/baobaobaivault&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  max_file_size</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">10737418240</span></span></code></pre></div><p>Notes:</p><ul><li><code>storage.temp_dir</code> is used when the backend temporarily buffers uploads before sending them to object storage</li><li><code>storage.max_file_size</code> still limits upload size behavior in the backend</li><li>keep local backend storage mounted during rollout if you enable local fallback for historical files</li></ul><h2 id="_4-recommended-rollout-order" tabindex="-1">4. Recommended Rollout Order <a class="header-anchor" href="#_4-recommended-rollout-order" aria-label="Permalink to &quot;4. Recommended Rollout Order&quot;">​</a></h2><p>Recommended production order:</p><ol><li>Keep the current local storage deployment unchanged</li><li>Add and verify your object storage config in <code>System Management -&gt; Storage Config</code></li><li>Create namespaces in <code>System Management -&gt; Namespaces</code></li><li>Open <code>System Management -&gt; Media Storage</code></li><li>Keep <code>Local fallback</code> enabled</li><li>Switch <code>Storage mode</code> from <code>local</code> to <code>object_storage</code></li><li>Select: <ul><li>one namespace for covers</li><li>one namespace for attachments</li></ul></li><li>Save the settings</li><li>Upload a few new test cards and verify: <ul><li>cover preview works</li><li>attachment download works</li><li>old cards still open correctly</li></ul></li></ol><h2 id="_5-recommended-namespace-design" tabindex="-1">5. Recommended Namespace Design <a class="header-anchor" href="#_5-recommended-namespace-design" aria-label="Permalink to &quot;5. Recommended Namespace Design&quot;">​</a></h2><p>A practical layout is:</p><ul><li>cover namespace: <ul><li>dedicated to smaller image files</li><li>can have stricter size limits</li></ul></li><li>attachment namespace: <ul><li>used for share card slot files</li><li>can allow larger file sizes</li></ul></li></ul><p>You can use:</p><ul><li>separate namespaces on the same bucket</li><li>or separate buckets or storage configs if your operations model prefers that</li></ul><h2 id="_6-current-backend-behavior" tabindex="-1">6. Current Backend Behavior <a class="header-anchor" href="#_6-current-backend-behavior" aria-label="Permalink to &quot;6. Current Backend Behavior&quot;">​</a></h2><p>Current implementation behavior:</p><ul><li>new uploads write according to <code>System Management -&gt; Media Storage</code></li><li>in <code>object_storage</code> mode, covers and attachments use <strong>browser direct upload to OSS</strong>: <ul><li>the backend generates a signed PUT URL (presign) and returns it to the frontend</li><li>the browser PUTs the file directly to OSS</li><li>after upload, the frontend calls the complete endpoint and the backend writes object metadata into <code>objects</code> / <code>object_versions</code></li></ul></li><li>in <code>local</code> mode, files are still uploaded through the backend to local disk</li><li>old records are read according to each row&#39;s recorded storage backend</li><li>local files can still be used as fallback when enabled</li><li>public media URLs remain unchanged</li><li>downloads and previews are still streamed by the backend (bucket remains private)</li></ul><p>That means you do not need to change share card URLs after switching.</p><h2 id="_7-system-page-fields" tabindex="-1">7. System Page Fields <a class="header-anchor" href="#_7-system-page-fields" aria-label="Permalink to &quot;7. System Page Fields&quot;">​</a></h2><p>The new page is:</p><ul><li><code>/system/media-storage</code></li></ul><p>Fields:</p><ul><li><code>Storage mode</code><ul><li><code>local</code>: new uploads keep writing to local disk</li><li><code>object_storage</code>: new uploads write to object storage</li></ul></li><li><code>Local fallback</code><ul><li>recommended <code>enabled</code> during migration period</li><li>allows old local files to continue serving if needed</li></ul></li><li><code>Cover namespace</code><ul><li>namespace used for newly uploaded card covers</li></ul></li><li><code>Asset namespace</code><ul><li>namespace used for newly uploaded card attachments</li></ul></li></ul><h2 id="_8-rollback-strategy" tabindex="-1">8. Rollback Strategy <a class="header-anchor" href="#_8-rollback-strategy" aria-label="Permalink to &quot;8. Rollback Strategy&quot;">​</a></h2><p>If you want to stop writing new media to OSS:</p><ol><li>Return to <code>System Management -&gt; Media Storage</code></li><li>Change <code>Storage mode</code> back to <code>local</code></li><li>Save</li></ol><p>Result:</p><ul><li>new uploads go back to local disk</li><li>previously uploaded OSS files still remain readable</li><li>historical local files are unaffected</li></ul><p>This is possible because storage location is recorded per file row.</p><h2 id="_9-deployment-notes" tabindex="-1">9. Deployment Notes <a class="header-anchor" href="#_9-deployment-notes" aria-label="Permalink to &quot;9. Deployment Notes&quot;">​</a></h2><p>During the mixed local + OSS stage, keep these in place:</p><ul><li>backend local storage volume mount</li><li>object storage config and credentials</li><li>namespaces used by media storage</li></ul><p>Do not remove local storage volume immediately after switching to OSS, because:</p><ul><li>historical local files may still exist</li><li>rollback may require local writes again</li><li>fallback reads may still be needed</li></ul><h2 id="_10-suggested-verification-checklist" tabindex="-1">10. Suggested Verification Checklist <a class="header-anchor" href="#_10-suggested-verification-checklist" aria-label="Permalink to &quot;10. Suggested Verification Checklist&quot;">​</a></h2><p>Before enabling object storage mode, confirm:</p><ul><li>[ ] Bucket CORS is configured (production domain plus local <code>http://localhost:3002</code>)</li><li>[ ] RAM sub-account is granted <code>oss:PutObject</code>, <code>oss:GetObject</code>, <code>oss:DeleteObject</code>, <code>oss:ListObjects</code></li><li>[ ] If using <code>acs:SourceIp</code> restriction, the local development IP is whitelisted</li></ul><p>After enabling object storage mode, verify all of the following:</p><ol><li>Create a new card with a cover</li><li>Create a new card bundle with attachments</li><li>Open card detail page and preview the cover</li><li>Download one attachment</li><li>Reopen an old card created before the switch</li><li>Delete and replace one cover</li><li>Delete and replace one attachment</li></ol><p>If all seven steps work, the first rollout is usually safe.</p><h2 id="_11-historical-file-migration" tabindex="-1">11. Historical File Migration <a class="header-anchor" href="#_11-historical-file-migration" aria-label="Permalink to &quot;11. Historical File Migration&quot;">​</a></h2><p>This first-stage implementation does not migrate historical local files automatically.</p><p>If you later want full migration, the next phase should include:</p><ol><li>scan old local share card media rows</li><li>upload them to object storage</li><li>backfill storage backend / namespace / object key fields</li><li>verify media reads</li><li>optionally remove old local files in batches</li></ol><p>It is safer to do that as a dedicated migration tool, not during normal upload requests.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("en/backend/share-media-oss.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const shareMediaOss = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  shareMediaOss as default
};
